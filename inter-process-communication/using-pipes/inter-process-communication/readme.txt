Using the C programming language, create two Linux processes that communicate with each other using pipes.
One process reads lines from an arbitrary file and sends them to the other process using a pipe.
The second process receives data from the pipe and prints it to the standard output.
