Using the C programming language, write a UNIX/Linux program that prevents the current process from being interrupted by a single press of the Ctrl-C key combination.
The process should only be terminated when this key combination is pressed twice in a row.
