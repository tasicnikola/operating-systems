
Using the C programming language, create two Linux processes.
The first process creates a matrix of dimensions MxN in a shared memory segment.
The second process finds the maximum element in each column of the matrix and prints it on the screen.
